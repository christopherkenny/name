# sort_phrase errors when element lacks phrase at loc

    Code
      sort_phrase(c("pop_2020", "plain"), loc = 2)
    Condition
      Error in `sort_phrase()`:
      ! `sort_phrase()` only works when every element has a phrase for `loc`.

