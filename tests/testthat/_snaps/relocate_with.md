# relocate_with errors with both .before and .after

    Code
      relocate_with(tb, sort, .before = a, .after = c)
    Condition
      Error in `relocate_with()`:
      ! May not supply both of `.before` and `.after`.

